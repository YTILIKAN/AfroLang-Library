# URL directe du backend (proxy Next.js + SSR)
API_URL=http://127.0.0.1:8000

# Sur Railway (service frontend) : URL publique du service backend-api
# API_URL=https://votre-backend.up.railway.app

# Optionnel — liens docs/API visibles dans l'interface
# NEXT_PUBLIC_API_URL=https://votre-backend.up.railway.app
